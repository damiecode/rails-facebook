<nav class="navbar navbar-expand-lg navbar-dark bg-primary mb-5">
  <%= link_to 'Rails Facebook', posts_path, method: :get, class: 'navbar-brand', id: 'logo' %>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
    <% if signed_in? %>
      <li class="nav-link">
        <%= link_to 'New Post', new_post_path(current_user), class:'text-white' %>
      </li>
      <li class="nav-link">
        <%= link_to 'Users',users_path, class:'text-white' %>
      </li> 
      <li class="nav-link">
        <%= link_to 'Friends Request',friendships_path, class:'text-white' %>
      </li> 
     <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle text-white ml-5" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          Profile
        </a>
       <div class="dropdown-menu" aria-labelledby="navbarDropdown">
            <%= link_to 'View profile', user_registration_path(current_user), class: 'dropdown-item' %>
            <div class="dropdown-divider"></div>
            <%= link_to 'Edit profile', edit_user_registration_path(current_user), class: 'dropdown-item' %>
        </div>
      </li>
     <div>
     </ul>
        <% if notice_count %>
          <li class="nav-link">
            <%= link_to "Delete notifications", notifications_destroy_path,class: 'text-white' %>
          </li>
        <% construct_notices.each do |notice| %>
          <li class=' d-block alert alert-success' role='alert'><%= link_to notice, friendships_path, method: :get %>
          </li>
      <% end %>
     </div>
    <%end%>

    <li class="nav-link">
      <%= link_to 'Log out', destroy_session_path(current_user), method: :delete,class: 'text-white' %>
    </li>
    <%else%>
      <li class="nav-link">
        <%= link_to 'Log in', new_session_path(@user),class: 'text-white' %>
      </li>
      <li class="nav-link">
        <%= link_to 'Sign Up', new_user_registration_path,class: 'text-white' %>
      </li>
    <%end%>
  </div>
</nav>